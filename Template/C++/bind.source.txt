#include <fb/model/container.h>

{{~ for table in tables ~}}
// __{{ table.name }} class
fb::model::__{{ table.name }}::__{{ table.name }}() : {{ table.type }}<{{ table.generic }}>(std::string("{{ table.name }}.json"))
{ }

fb::model::__{{ table.name }}::~__{{ table.name }}()
{ }
{{~ if !for.last }}


{{ end ~}}
{{~ end ~}}