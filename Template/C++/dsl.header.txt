class fb::model::dsl::{{ name }}
{
{{~ if params.size > 0 ~}}
public:
{{~ for param in params ~}}
    const {{ param.type }} {{ param.name }};
{{~ end ~}}

{{ end ~}}
public:
    {{ name }}({{ for param in params }}{{ param.rtype }} {{ param.name }}{{ if !for.last }}, {{ end }}{{ end }});
    {{ name }}(const Json::Value& json);
    {{ name }}(const std::vector<std::any>& parameters);

public:
    fb::model::dsl to_dsl();
};