{{~ for table in tables ~}}
    function {{ table.name }}ContainerBuilder() {
        return {
            build: function (v) {
                return {{ table.type }}(v)
            }
        }
    }{{ if !for.last }}
{{ end }}
{{~ end ~}}