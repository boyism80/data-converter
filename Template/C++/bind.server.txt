#ifndef __MODEL_BIND_{{ scope | string.upcase }}_H__
#define __MODEL_BIND_{{ scope | string.upcase }}_H__

#include <fb/model/{{ scope | string.downcase }}.h>

namespace fb { namespace model { namespace {{ scope | string.downcase }} {

{{~ for table in tables ~}}
class {{ table.name }}s : public {{ table.type }}<{{ table.generic }}>
{
public:
    {{ table.name }}s() : {{ table.type }}<{{ table.generic }}>("{{ table.name }}.json")
    {}
    ~{{ table.name }}s() = default;
};
{{~ if !for.last }}
{{ end ~}}
{{~ end ~}}


class container
{
public:
{{~ for table in tables ~}}
    fb::model::{{ table.name }}s {{ table.name }};
{{~ end ~}}
};

} } }

#endif