{{~
func ns(namespaces)
    ret namespace | array.join "."
end
~}}
{{~ for item in items ~}}
    public class {{ item.name }}{{ if item.based }} : {{ ns(namespace) }}.{{ item.based }}{{ end }}
    {
{{~ for prop in item.props ~}}
        public {{ prop.type }} {{ prop.name }} { get; set; }
{{~ end ~}}
    }
{{~ if !for.last }}
{{ end ~}}
{{~ end ~}}
