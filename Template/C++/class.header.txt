// {{ name }} class
{{~ if super ~}}
class {{ name }}
{{~ else ~}}
class {{ name }}{{ if inherit }} : public fb::model::common::{{ name }}{{ end }}
{{~ end ~}}
{
{{~ if properties.size > 0 ~}}
public:
{{~ for property in properties ~}}
    const {{ property.type }} {{ property.name }};
{{~ end ~}}

{{~ end ~}}
public:
    {{ name }}(const Json::Value& json);
    ~{{ name }}();
};