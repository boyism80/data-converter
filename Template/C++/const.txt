namespace consts { 

{{~ for item in items ~}}
class {{ item.key }}
{
public:
{{~ for prop in item.value ~}}
    inline static const {{ prop.type }} {{ prop.name }} = {{ prop.value }};
{{~ end ~}}

private:
    {{ item.key }}() = default;
    ~{{ item.key }}() = default;
};
{{~ end ~}}

}